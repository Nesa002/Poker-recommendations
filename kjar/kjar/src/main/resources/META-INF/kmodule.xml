<kmodule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://jboss.org/kie/6.0.0/kmodule">

    <!-- KieBase za backward chaining -->
    <kbase name="foldRulesBase" packages="rules.fold">
        <ksession name="foldRulesSession"/>
    </kbase>

    <!-- KieBase za forward chaining -->
    <kbase name="forwardRulesBase" packages="rules.forward">
        <ksession name="forwardRulesSession"/>
    </kbase>

    <kbase name="handEval" packages="rules.hand_eval">
        <ksession name="handEvalSession"/>
    </kbase>

</kmodule>